This directory contains some android samples available for public.
The git repo uses 'androidhahn'
Note that domain name is 'github-smugmug' instead of github.com. 
Why?  Because we have the following ~/.ssh/config file specifying


Host github-smugmug
	HostName github.com
	User androidhahn
	IdentityFile ~/.ssh/id_rsa.work

To add your git repo to remote, use :

  git add your-file
  git commit -m "your description"
  git remote add origin git@github-smugmug:androidhahn/AndroidSamples.git
  git push -u origin master

To clone this :

  git clone git@github-smugmug:androidshahn/AndroidSamples.git


